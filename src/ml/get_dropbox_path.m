function [ path ] = get_dropbox_path( )
%GET_DROPBOX_PATH - create a configurable file that each teammate can edit
%once to point to their folder, and that won't get updated afterwards.
%
% OUTPUT:
%   path - the absolute path to the dropbox data shared amongst the team.

path = '';

end

